// Application constants
const MAX_IMAGES = 50;
const MAX_TIME_MS = 3 * 60 * 1000; // 3 minutes
const USER_AGENT = 'ImageCrawlerBot/1.0';

module.exports = {
  MAX_IMAGES,
  MAX_TIME_MS,
  USER_AGENT
};